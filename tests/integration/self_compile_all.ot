// Test self-compilation of all bootstrap modules
console.log("=== Self-Compilation Test ===");

// Compile types.ot
let src1 = File.readSync("bootstrap/types.ot");
let r1 = compileToBytecode(src1);
console.log("types.ot: " + r1.length + " bytes");

// Compile lexer.ot
let src2 = File.readSync("bootstrap/lexer.ot");
let r2 = compileToBytecode(src2);
console.log("lexer.ot: " + r2.length + " bytes");

// Compile parser.ot
let src3 = File.readSync("bootstrap/parser.ot");
let r3 = compileToBytecode(src3);
console.log("parser.ot: " + r3.length + " bytes");

// Compile emitter.ot
let src4 = File.readSync("bootstrap/emitter.ot");
let r4 = compileToBytecode(src4);
console.log("emitter.ot: " + r4.length + " bytes");

// Compile ir.ot
let src5 = File.readSync("bootstrap/ir.ot");
let r5 = compileToBytecode(src5);
console.log("ir.ot: " + r5.length + " bytes");

// Compile ir_builder.ot
let src6 = File.readSync("bootstrap/ir_builder.ot");
let r6 = compileToBytecode(src6);
console.log("ir_builder.ot: " + r6.length + " bytes");

// Compile codegen.ot
let src7 = File.readSync("bootstrap/codegen.ot");
let r7 = compileToBytecode(src7);
console.log("codegen.ot: " + r7.length + " bytes");

// Compile pipeline.ot
let src8 = File.readSync("bootstrap/pipeline.ot");
let r8 = compileToBytecode(src8);
console.log("pipeline.ot: " + r8.length + " bytes");

console.log("=== Done ===");
